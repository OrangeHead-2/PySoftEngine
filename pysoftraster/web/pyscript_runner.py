def run_scene(scene, canvas_id="output"):
    # This is a stub for PyScript, would call the scene.render() pipeline and
    # update a HTML canvas via PyScript/Pyodide.
    print(f"Rendering scene {scene} to canvas {canvas_id}. (PyScript stub)")